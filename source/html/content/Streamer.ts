import Article from "../article"
import { p } from "../utils"

export default new Article("Streamer",p("Is this real, chat?"))
.addSection(
    "How it started",
    p(`
        So one day I was like "man, playing games alone is kinda boring..." but then you might go: "why don't just play with friends?".
    `),
    p(`
        Yes, of course I did that but most of these online games are not my taste
    `)
)